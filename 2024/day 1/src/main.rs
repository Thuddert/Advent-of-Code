// use std::fs::File;
// use std::io::{BufRead, BufReader};

// pub fn calc_smallest_distance(left: Vec<i32>, right: Vec<i32>) -> i32 {
//     // 1. sorts vec
//     // 2. measures the distance between elements
//     // 3. sums up the distances
//     return 1
// }



fn main() {
    // 1. read day1.txt
    // 2. split the text in two columns
    // 3. save the splitted text as vectors
    // 4. calculate smallest distance
}
